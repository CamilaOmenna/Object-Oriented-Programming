const prompt = require('prompt-sync')();

var Produto = /** @class */ (function () {
    function Produto() {
    }
    return Produto;
}());

var item = new Produto();
item.marca = 'camera';
item.modelo = 'Nike';
item.cor = 'preto';
item.fotografar = 'Ultilizar para fotografar';

console.log(`Marca: ${item.marca}`)
console.log(`Modelo: ${item.modelo}`)
console.log(`Cor: ${item.cor}`)
console.log(`Funcionalidade: ${item.fotografar}`)

document.write("".concat(item.marca,));
document.write("".concat(item.modelo,));
document.write("".concat(item.cor,));
document.write("".concat(item.pagarContas,));
document.write("".concat(item.ligacoes));
