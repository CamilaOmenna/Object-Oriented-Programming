const prompt = require('prompt-sync')();

var Cliente = (function(){
    function Cliente() {}
    return Cliente;}());

var usuario = new cliente();

Cliente.nome = 'Camila linda';
Cliente.cpf = '1268.8993.827.-8';
Cliente.compra = 'Batata, alface,macarrão, melão ,morango';

console.log(`Nome: ${usuario.nome}`);
console.log(`CPF: ${usuario.cpf}`);
console.log(`Compra: ${usuario.compra}`);

document.write("".concat(usuario.nome,));
document.write("".concat(usuario.cpf, ));
document.write("".concat(usuario.compra));
